/**
 * Centralized lookback tooltips for UI.
 * Re-exports from lookbackLabels so pages import from one place.
 */

export { getT6Tooltip, getT12Tooltip, getLookbackRangeLabel } from './lookbackLabels';
