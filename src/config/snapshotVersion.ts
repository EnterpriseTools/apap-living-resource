/**
 * Snapshot versioning for stored processed data.
 * Bump COMPUTE_VERSION when adoption/eligibility or metric computation rules change.
 */

export const SNAPSHOT_SCHEMA_VERSION = 1;
export const COMPUTE_VERSION = 2;
